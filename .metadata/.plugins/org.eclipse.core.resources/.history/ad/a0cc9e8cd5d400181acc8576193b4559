package JOB;

public class _10_18_Iterator_Demo {
	public static void main(String[] args) {
		//提供一个方法按顺序遍历一个集合内部元素，而不暴露该对象的内部表示
		/**
		 * 应用场景
		 * 访问一个聚合的对象，而不暴露对象的内部表示
		 * 支持对聚合对象的多种遍历
		 * 对遍历不同的对象提供统一的接口
		 * 迭代器设计模式的角色构成
		 * 迭代器角色，具体迭代器角色，容器角色，具体容器角色
		 */
	}

}
